#include "main.h"
#include <string.h>

/**
 * _strcmp - Compares two srings
 * @s1: String to compare
 * @s2: string to compare
 *
 * Return: An integer based on comparison levels
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
